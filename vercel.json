{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/backend/(.*)",
      "destination": "${env.BACKEND_ORIGIN}/$1"
    },
    {
      "source": "/api/backend",
      "destination": "${env.BACKEND_ORIGIN}"
    }
  ]
}
